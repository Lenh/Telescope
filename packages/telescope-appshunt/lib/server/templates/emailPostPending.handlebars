<span class="heading">
Hi <b>{{postAuthorName}}</b><br/>
thank you for posting: 
<br/><br/>

Your post {{#if url}}
  <a href="{{linkUrl}}" class="action-link">{{postTitle}}}</a>
{{else}}
  {{postTitle}}}
{{/if}} is waiting for approval. We will notify you by email as soon as it will get online.
</span><br><br>

{{#if htmlBody}}
  <div class="post-body">
  {{{htmlBody}}}
  </div>
  <br>
{{/if}}
